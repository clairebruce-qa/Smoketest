import com.thoughtworks.selenium.*;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

import org.junit.runner.RunWith;
import org.junit.runners.Parameterized;
import org.junit.runners.Parameterized.Parameters;
import org.junit.*;
import lib.*;


import java.util.regex.Pattern;
//require for the collection class
import java.util.*;

@RunWith(value=Parameterized.class)
public class UsersLogin {

private static DefaultSelenium selenium;
private String user;
private String password;

@BeforeClass
public static void init() /* throws Exception */ {
selenium = new DefaultSelenium("localhost", 4444, "*iehta","https://certi.telefonica.net/");
selenium.start();
selenium.windowMaximize();
selenium.windowFocus();
}


@AfterClass
public static void stop(){
	selenium.stop();
}

@Parameters
public static Collection data() {
 return Arrays.asList( new Object[][] {
                      { "usuario_tb_1", "prueba" },   // expected, value
                      { "usuario_tb_2", "prueba" },
                      { "usuario_tb_3", "prueba" },
                      { "usuario_tb_4", "prueba" },
                      { "usuario_tb_5", "prueba" },//wrong expected
                      });
}

public UsersLogin(String expected, String value) {
 this.user = expected;
 this.password = value;
}
@Test
public void login() throws Exception  {

	Tb.login(selenium, user, password);
	
	Tb.setLanguageEN(selenium);
	
	Tb.logout(selenium);
	
}
}
